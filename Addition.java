public class Addition extends MultiAddition {

    /**
     * This class represents an arithmetical expression that
     * results after adding two expressions.
     * */
    /** This constructs the Addition object based on two given expression.
     * @param val1 - The first argument
     * @param val2 - The Second argument
     *              that are added together.
     */
    public Addition(Expression val1, Expression val2) {
        super(val1, val2);
    }
}